"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function LoginPage(){const[busy,setBusy]=useState(false),[erro,setErro]=useState("");async function entrar(){setBusy(true);const{error}=await createClient().auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}});if(error){setErro(error.message);setBusy(false)}}return <main className="flex min-h-screen items-center justify-center bg-base-bg px-4"><div className="w-full max-w-sm rounded-2xl border border-base-border bg-base-surface p-8"><span className="brand-mark !h-20 !w-20 !rounded-2xl">FE</span><p className="eyebrow mt-8">FOCO & ESCUDO</p><h1 className="mt-2 text-2xl font-bold">Agenda de serviços</h1><p className="mt-2 text-sm leading-6 text-ink-muted">Instalações, retiradas e manutenções organizadas em um só lugar.</p><button onClick={entrar} disabled={busy} className="btn-secondary mt-8 w-full">{busy?"Conectando...":"Entrar com Google"}</button>{erro&&<p className="mt-3 text-xs text-red-300">{erro}</p>}<p className="mt-6 text-[10px] leading-4 text-ink-faint">Sua sessão permanece ativa e só expira após 5 dias sem abrir o aplicativo.</p><div className="mt-3 flex justify-center gap-3 text-[10px] text-ink-faint"><a href="/privacidade" className="hover:text-amber">Privacidade</a><a href="/termos" className="hover:text-amber">Termos</a></div></div></main>}
