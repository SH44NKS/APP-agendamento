import {createClient} from "@/lib/supabase/server";import {OSCard} from "@/components/OSCard";import {LogoutButton} from "@/components/LogoutButton";
export default async function TecnicoPage(){const s=createClient();const{data:{user}}=await s.auth.getUser();const[{data:ordens},{data:perfil}]=await Promise.all([s.from("ordens_servico").select("*").eq("tecnico_id",user?.id).order("criado_em",{ascending:false}),s.from("profiles").select("nome").eq("id",user?.id).single()]);const lista=ordens??[],abertas=lista.filter(o=>o.status!=="concluido"&&o.status!=="cancelado").sort((a,b)=>Number(b.prioridade==="alta")-Number(a.prioridade==="alta")),concluidas=lista.filter(o=>o.status==="concluido");return <main className="min-h-screen bg-base-bg px-4 py-7"><div className="mx-auto max-w-md"><header className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="brand-mark">FE</span><div><b className="text-sm">APP agendamento</b><p className="text-[10px] text-ink-faint">Foco & Escudo</p></div></div><LogoutButton/></header><h1 className="mt-9 text-2xl font-bold">Olá, {perfil?.nome?.split(" ")[0]??"técnico"}</h1><p className="mt-1 text-sm text-ink-muted">Seus serviços em aberto, com prioridades altas primeiro.</p><div className="mt-6 flex flex-col gap-3">{abertas.map(os=><OSCard key={os.id} os={os as any}/>)}{abertas.length===0&&<div className="empty-state">Nenhum serviço em aberto no momento.</div>}</div>{concluidas.length>0&&<details className="mt-8"><summary className="cursor-pointer text-sm font-semibold text-ink-muted">Concluídos ({concluidas.length})</summary><div className="mt-3 flex flex-col gap-3">{concluidas.map(os=><OSCard key={os.id} os={os as any}/>)}</div></details>}</div></main>}
