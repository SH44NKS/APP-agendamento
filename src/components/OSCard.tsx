import Link from "next/link"; import {StatusBadge} from "./StatusBadge"; import {OrdemServico,TIPO_LABEL,diasPendente,statusVisual} from "@/lib/os";
export function OSCard({os,amarelo=3,vermelho=7}:{os:OrdemServico;amarelo?:number;vermelho?:number}){const dias=diasPendente(os),visual=statusVisual(os,amarelo,vermelho);return <Link href={`/os/${os.id}`} className="os-card group">
  <div className="flex items-start justify-between gap-2"><span className="service-label">{TIPO_LABEL[os.tipo]}</span><StatusBadge status={visual}/></div>
  <div className="my-4 border-t border-dashed border-base-border"/><h3 className="truncate font-semibold">{os.cliente_nome}</h3><p className="mt-1 truncate font-mono text-xs text-ink-muted">{os.veiculo_modelo} · {os.veiculo_identificador}</p><p className="mt-3 truncate text-xs text-ink-muted">📍 {os.local}</p>{os.tecnico?.nome&&<p className="mt-1 truncate text-xs text-ink-muted">👷 {os.tecnico.nome}</p>}
  <div className="mt-4 flex items-end justify-between gap-3 text-[11px] text-ink-faint"><span>Consultor: {os.consultor_nome}</span>{os.status==="pendente"&&<span className={visual==="critico"?"text-red-300":visual==="atrasado"?"text-amber":""}>{dias===0?"Hoje":`${dias}d em aberto`}</span>}</div>
</Link>}
