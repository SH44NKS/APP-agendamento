import {createServerClient,type CookieOptions} from "@supabase/ssr";import {NextResponse,type NextRequest} from "next/server";
const CINCO_DIAS=5*24*60*60*1000;
export async function middleware(request:NextRequest){let response=NextResponse.next({request:{headers:request.headers}});const supabase=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{get(name:string){return request.cookies.get(name)?.value},set(name:string,value:string,options:CookieOptions){response.cookies.set({name,value,...options})},remove(name:string,options:CookieOptions){response.cookies.set({name,value:"",...options})}}});const{data:{user}}=await supabase.auth.getUser();const path=request.nextUrl.pathname;const publico=path==="/"||path==="/login"||path==="/privacidade"||path==="/termos"||path.startsWith("/auth");const agora=Date.now(),ultima=Number(request.cookies.get("last_activity")?.value??0);const verificar=!publico||path==="/login";if(user&&verificar&&ultima>0&&agora-ultima>CINCO_DIAS)return NextResponse.redirect(new URL("/auth/logout?motivo=inatividade",request.url));if(!user&&!publico)return NextResponse.redirect(new URL("/login",request.url));if(user&&verificar)response.cookies.set("last_activity",String(agora),{httpOnly:true,sameSite:"lax",secure:request.nextUrl.protocol==="https:",path:"/",maxAge:30*24*60*60});if(user&&path==="/login")return NextResponse.redirect(new URL("/dashboard",request.url));return response}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico|app-icon.png|pdf.worker.min.mjs|sw.js|manifest.webmanifest).*)"]};
